{
  "name": "time-sync",
  "version": "1.0.0",
  "description": "Server for syncronizing time",
  "main": "app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "timesync": "^0.2.0"
  }
}
